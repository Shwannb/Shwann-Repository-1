export default function HomePage() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'monospace' }}>
      <h1>Safyr Deal Terminal</h1>
      <p>Phase 1 — API routes live at /api/deals and /api/news. UI pending.</p>
    </main>
  );
}
